const colors = require('tailwindcss/colors')

// foreground primary: white
// foreground secondary: #707070
// bg-[#171717] border-[#505050]


module.exports = {
  theme: {
    colors: {
      gray: colors.coolGray,
      blue: colors.lightBlue,
      red: colors.rose,
      pink: colors.fuchsia,
    },
  }
}